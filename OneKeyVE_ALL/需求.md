# 历史需求归档

本文档为历史需求集合，保留项目演进过程中的多版需求描述。

说明：

- 该文档主要用于追踪需求演进，不应再直接作为当前实现规范。
- 当前代码审计与 Go 重构以 `ve_wallpaper_double_release.py` 对应链路为主。
- 当前推荐阅读顺序为：`ReadMe.md` -> `AUDIT.md` -> `需求20260128.md`。

---

## 方案一：仅用 FF 处理，不包含前景羽化

"""
用python程序完成需求，要求使用纯FFmpeg实现，程序的异常处理能力和鲁棒性都要考虑到：
0. 视频文件可能是1个或者多个，所以是处理批量视频文件。当前程序的文件名是Video_Edit_FF.py（含后缀），我希望直接运行：python Video_Edit_FF.py 即可自动完成所有功能，而不是需要我额外的添加视频文件的文件名才可以。
1. 判断视频文件比例是否为9:16或者16:9，如果不是则进行2。如果是则跳过此视频文件的处理。
2. 我们将视频比例如9:16看作是一个数学上的分数，如9:16就是分数9/16，
   如果视频比例大于9/16则进行3，如果视频比例小于9/16则进行4。
3. 新建一个临时视频，画幅设定为9：16，画幅分辨率中的宽度同原视频的宽度。然后进行5。
4. 新建一个临时视频，画幅设定为9：16，画幅分辨率中的高度同原视频的高度。然后进行5。
5. 将原视频拷贝到临时视频中，拷贝过来的原视频的中心点应当和画幅的中心点重合，此时形成轨道01，轨道01以中心点不变等比例放大100%。
裁切掉超出画幅的部分以节省文件体积。对轨道01进行高斯模糊，模糊参数就用常用值即可。
再次拷贝原视频到临时视频中，同样拷贝过来的原视频的中心点应当和画幅的中心点重合，形成轨道02.
临时视频的轨道02导入原视频之后，正确的状态是原视频的中心点和临时视频的画幅的中心点重合。轨道02无需进行任何缩放操作。由于轨道02上的视频在此处理逻辑中必然会导致填不满宽度或者高度其中一个维度，这些填不满的部分会默认用黑色填充，将这些用黑色填充的部分裁切掉，这样轨道02裁切掉的部分就可以正常显示出轨道01的内容。
之后对裁切后的轨道02边缘做羽化处理，羽化值为常用值。
6. 保证所有轨道可见。合成视频到当前目录下的output目录（若没有则新建），
   导出的视频文件名为原视频文件名。

99.以上流程处理完之后，检查格式错误，检查语法错误，检查拼写错误，检查调用错误，修改完成后给我完整的代码。

999.加上详细注释

"""


--------------------------------


## 方案二：用 FF 处理画面，导出使用 CUDA

'''

用python程序完成需求，要求使用纯FFmpeg实现，能用cuda加速处理的部分用cuda处理。程序的异常处理能力和鲁棒性都要考虑到：
0. 视频文件可能是1个或者多个，所以是处理批量视频文件。
当前程序的文件名是Video_Edit_FF.py（含后缀），我希望直接运行：python Video_Edit_FF.py 
即可自动完成所有功能，而不是需要我额外的添加视频文件的文件名才可以。

1. 判断视频文件比例是否为9:16或者16:9，如果不是则进行2。如果是则跳过此视频文件的处理。

2. 我们将视频比例如9:16看作是一个数学上的分数，如9:16就是分数9/16， 
   如果视频比例大于9/16则进行3，如果视频比例小于9/16则进行4。

3. 新建一个临时视频，画幅设定为9：16，画幅分辨率中的宽度同原视频的宽度。然后进行5。

4. 新建一个临时视频，画幅设定为9：16，画幅分辨率中的高度同原视频的高度。然后进行5。

5. 将原视频拷贝到临时视频中，拷贝过来的原视频的中心点应当和画幅的中心点重合，此时形成轨道01， 
   轨道01以中心点不变等比例放大100%。裁切掉超出画幅的部分以节省文件体积。 
   对轨道01进行高斯模糊，模糊参数就用常用值即可。 
   
   再次拷贝原视频到临时视频中，同样拷贝过来的原视频的中心点应当和画幅的中心点重合，形成轨道02.
   临时视频的轨道02导入原视频之后，正确的状态是原视频的中心点和临时视频的画幅的中心点重合。 
   轨道02无需进行任何缩放操作。 
   
   由于轨道02上的视频在此处理逻辑中必然会导致填不满宽度或者高度其中一个维度， 
   这些填不满的部分会默认用黑色填充，将这些用黑色填充的部分裁切掉， 
   这样轨道02裁切掉的部分就可以正常显示出轨道01的内容。对于轨道02未被裁切的部分进行边缘羽化，仅对轨道02画面四周最外侧的5个像素宽度区域进行透明度渐变处理。在这个5像素宽的边缘区域内，透明度应从画面最外边缘的0%（完全透明）平滑过渡到距离边缘5像素处的100%（完全不透明）。羽化过渡曲线应采用线性渐变方式。画面内部（距离边缘超过5像素的区域）应保持100%不透明，不受任何影响。

   

6. 保证所有轨道可见。 
   合成视频到当前目录下的output目录（若没有则新建）， 导出的视频文件名为原视频文件名。合成（导出）视频可以用cuda加速以加快处理速度。

7.显存使用允许最高使用4GB。

99.以上流程处理完之后，检查格式错误，检查语法错误，检查拼写错误，检查调用错误，修改完成后给我完整的代码。
999.加上详细注释

'''




-------------------------------------------------------


## 方案三：用其他 Python 库实现

'''

用python程序完成需求，显存使用允许最高使用4GB。根据需求单独或者配合使用VidGear ffmpeg-python 这两个库完成以上所有功能需求的来。要兼顾速度和质量,尽可能多的用gpu处理来加快处理速度。能用cuda加速处理的部分用cuda处理。程序的异常处理能力和鲁棒性都要考虑到：
0. 视频文件可能是1个或者多个，所以是处理批量视频文件。
当前程序的文件名是Video_Edit_FF.py（含后缀），我希望直接运行：python Video_Edit_FF.py 
即可自动完成所有功能，而不是需要我额外的添加视频文件的文件名才可以。

1. 判断视频文件比例是否为9:16或者16:9，如果不是则进行2。如果是则跳过此视频文件的处理，跳过的视频无需拷贝到输出文件夹。

2. 我们将视频比例如9:16看作是一个数学上的分数，如9:16就是分数9/16， 
   如果视频比例大于9/16则进行3，如果视频比例小于9/16则进行4。

3. 新建一个临时视频，画幅设定为9：16，画幅分辨率中的宽度同原视频的宽度。然后进行5。

4. 新建一个临时视频，画幅设定为9：16，画幅分辨率中的高度同原视频的高度。然后进行5。

5. 将原视频拷贝到临时视频中，拷贝过来的原视频的中心点应当和画幅的中心点重合，此时形成轨道01， 
   轨道01以中心点不变等比例放大100%。裁切掉超出画幅的部分以节省文件体积。 
   对轨道01进行高斯模糊，模糊参数就用常用值即可。 
   
   再次拷贝原视频到临时视频中，同样拷贝过来的原视频的中心点应当和画幅的中心点重合，形成轨道02.
   临时视频的轨道02导入原视频之后，正确的状态是原视频的中心点和临时视频的画幅的中心点重合。 
   轨道02无需进行任何缩放操作。 
   
   由于轨道02上的视频在此处理逻辑中必然会导致填不满宽度或者高度其中一个维度， 
   这些填不满的部分会默认用黑色填充，将这些用黑色填充的部分裁切掉（重要）， 
   这样轨道02裁切掉的部分就可以正常显示出轨道01的内容。对于轨道02未被裁切的部分进行边缘羽化，仅对轨道02画面四周最外侧的25个像素宽度区域进行透明度渐变处理。在这个25像素宽的边缘区域内，透明度应从画面最外边缘的0%（完全透明）平滑过渡到距离边缘5像素处的100%（完全不透明）。羽化过渡曲线应采用线性渐变方式。画面内部（距离边缘超过5像素的区域）应保持100%不透明，不受任何影响。

   

6. 保证所有轨道可见。 
   合成视频到当前目录下的output目录（若没有则新建）， 导出的视频文件名为原视频文件名。合成（导出）视频可以用cuda加速以加快处理速度。



99.以上流程处理完之后，检查格式错误，检查语法错误，检查拼写错误，检查调用错误，检查逻辑错误。修改完成后给我完整的代码。
999.加上详细注释


-------------------------------------

## 方案四：制作合适比例的视频壁纸的关键方法

用python程序完成需求，显存使用允许最高使用4GB。使用纯ffmpeg实现。
要兼顾速度和质量,尽可能多的用gpu处理来加快处理速度,能用cuda加速处理的部分用cuda处理。
程序的异常处理能力和鲁棒性都要考虑到.
需要用的ffmpeg命令和滤镜的地方，要严格（非常重要，可以大幅度降低因版本不同造成的报错）使用ffmpeg_full_diagnostics.json中使用的规范！
视频文件可能是1个或者多个，所以是处理批量视频文件。
当前程序的文件名是xxx.py（含后缀），我希望直接运行：python xxx.py 
即可自动完成所有功能，而不是需要我额外的添加视频文件的文件名才可以。

依据以上前提条件，完成以下需求：

0. 遍历程序所在目录中的所有子目录和叶目录，找到ffmpeg所有需要的组件，并设置路径给后面所有的需求调用。

1. 判断原视频文件比例是否大于1：1，如果是则将视频顺时针旋转为90度作为新的原视频。即为保证原视频（含已旋转）的宽都小于高。

2. 将每个原视频（含已旋转）的宽度(标记为w)分别除以两个分数9/20（标记为h1）、5/11(标记为h2)作为高度，程序要当前原视频处理后输出的两个视频的画幅就分别为此视频的两个宽和高。

3. 新建2个临时视频，画幅的宽和高分别为w和h1（临时视频1）、w和h2（临时视频2）。之后的步骤同时对两个临时视频都生效（注意严格按照ffmpeg_full_diagnostics.json规范使用命令，如果VidGear、ffmpeg-python中有更简介可靠的表达方式优先用VidGear、ffmpeg-python）。

4. 在临时视频（1，2）中导入原视频（含已旋转）形成轨道1，将轨道1的视频放大100%，然后裁切掉超过画幅的部分，之后对轨道01使用高斯模糊，模糊的值为20（注意严格按照ffmpeg_full_diagnostics.json规范使用命令，如果VidGear、ffmpeg-python中有更简介可靠的表达方式优先用VidGear、ffmpeg-python）。

5. 在完成步骤4的临时视频（1，2）之后，再次创建一个临时视频3，临时视频3的画幅分辨率等于原视频分辨率（含已旋转），将原视频（含已旋转）导入临时视频3，之后对临时视频3进行收缩性边缘渐变羽化，羽化半径是30像素。   
对临时视频（1、2）导入临时视频3形成轨道2。轨道2中的视频应当居中。裁剪掉导入时自动产生的黑色填充背景（裁剪掉黑边，上下或左右，此步骤重要！）


6. 保证所有轨道可见。 
   合成视频将保存到对应画幅比例所对应output目录的子目录中，如临时视频1导出后放入9x20的子目录中，临时视频2导出后放入5x11的子目录中。导出时使用vbr，码率大概在10M左右。同时在整个处理流程中（步骤1-6）码率都保持在10M左右。（注意是使用可变动态码率。）

7. 全流程中能用gpu加速的地方要用gpu加速！（注意严格按照ffmpeg_full_diagnostics.json规范使用命令，如果VidGear、ffmpeg-python中有更简介可靠的表达方式优先用VidGear、ffmpeg-python）。加上详细注释

99.以上流程处理完之后，检查格式错误，检查语法错误，检查拼写错误，检查调用错误，检查逻辑错误。修改完成后给我完整的代码。




------------------------------

## 方案五：打包需求

'''

需求：
将目录下的py程序打包为一个exe文件。文件包含：文件名称前面的数字为我给它自定义的编号。编号后面的空格（不含空格）以后的部分为文件名和后缀名。
1 ve_wallpaper_double.py
2 VE_QW_FInal_prefect.py
3 Video_Edit_FF.py
4 VE_QW_all.py
5 ve_wallpaper.py

打包好的exe文件要有一个ui界面，界面为暗色调，主题配色为Darcula PyCharm Theme for VS Code同款（含标题栏）
界面第一行 是一个路径输入框，可以通过拖拽文件夹的方式确认目录。或者通过输入框后面的浏览按钮确认程序运行目录。当然也可以通过手动输入，手动输入应当确保直接从win的资源管理器复制的完整目录路径正常可用。

界面第二行为四个按钮，括号内为对应的py程序编号。
一生二（对应编号1的程序） 9x16mov（对应编号2的程序）纯FFcpu(对应编号3的程序) 貌似硬切（对应编号4的程序） 虽然硬切但很快（对应编号5）

第三行为处理进度条。

第四行为处理内容显示区域。（显示程序运行时命令行窗口信息。）

ui窗口的右上角 最小化按钮功能为最小化到win系统的右下角托盘。 关闭按钮为直接关闭程序（关闭按钮不能拥有最小化到win右下角托盘的功能）。

将以上编号1-5的py以“原封不动”的样子搬到主ui程序中去。

打包好的exe也应当包含对应的ffmpeg完整组件，无需在exe同级目录下包含ffmpeg文件夹或组件。打包时打包用的py程序统计目录含有需要的ffmpeg文件夹，含完整的ffmpeg目录结构和全部组件。

打包好的exe运行时默认处理目录应当是exe程序所在目录，而非释放资源时的临时目录。

根据以上需求给我完整的处理流程，以及详细的打包命令。


一键动态


python -m PyInstaller --noconfirm --onefile --windowed --name "一键动态" --add-data "ve_wallpaper_double.py;." --add-data "VE_QW_FInal_prefect.py;." --add-data "Video_Edit_FF.py;." --add-data "VE_QW_all.py;." --add-data "ve_wallpaper.py;." --add-data "ffmpeg;ffmpeg" main_ui.py


'''



## 方案六：打包单个 EXE

将ve_wallpaper_double.py打包为单个exe文件。

打包好的exe文件要有一个ui界面，界面为暗色调，主题配色为Darcula PyCharm Theme for VS Code同款（含标题栏）
界面第一行 是一个路径输入框，可以通过拖拽文件夹的方式确认目录。或者通过输入框后面的浏览按钮确认程序运行目录。当然也可以通过手动输入，手动输入应当确保直接从win的资源管理器复制的完整目录路径正常可用。

界面第二行为一个开始按钮


第三行为处理进度条。

第四行为处理内容显示区域。（显示程序运行时命令行窗口信息。）

ui窗口的右上角 最小化按钮功能为最小化到win系统的右下角托盘。 关闭按钮为直接关闭程序（关闭按钮不能拥有最小化到win右下角托盘的功能）。

打包好的exe也应当包含对应的ffmpeg完整组件，无需在exe同级目录下包含ffmpeg文件夹或组件。打包时打包用的py程序统计目录含有需要的ffmpeg文件夹，含完整的ffmpeg目录结构和全部组件。

打包好的exe运行时默认处理目录应当是exe程序所在目录，而非释放资源时的临时目录。

根据以上需求给我完整的处理流程，以及详细的打包命令。



python -m PyInstaller --noconsole --onefile --clean --name "一键动态" --icon="01.ico" --add-data "01.ico;." --collect-all PyQt6 main_gui_v3.py


python -m PyInstaller --noconsole --onefile --clean --name "一键动态_pre03" --icon="01.ico" --add-data "01.ico;." --collect-all PyQt6 main_gui_v3.py


python -m PyInstaller --noconsole --onefile --clean --name "OneKeyVE" --icon="01.ico" --add-data "01.ico;." --collect-all PyQt6 main_gui_v3.py
